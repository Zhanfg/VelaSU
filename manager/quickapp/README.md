# VelaSU Manager Quick App

This directory is built independently by `.github/workflows/build-quickapp.yml`.

- Debug: `npm run build`
- Release: `npm run release`

The initial UI is deliberately non-privileged. VelaCall integration is added only after the runtime bootstrap and caller-identity model are validated.
